# 🚀 Швидкий старт

Цей посібник допоможе вам швидко розпочати роботу з системою обліку студентів. Час на встановлення: **5-10 хвилин**.

## 📑 Зміст

- [Крок 1: Створення бази даних](#крок-1-створення-бази-даних)
- [Крок 2: Створення таблиць](#крок-2-створення-таблиць)
- [Крок 3: Завантаження тестових даних](#крок-3-завантаження-тестових-даних)
- [Крок 4: Перевірка встановлення](#крок-4-перевірка-встановлення)
- [Варіанти встановлення](#варіанти-встановлення)

---

## Крок 1: Створення бази даних

### Використовуючи MySQL CLI

```bash
mysql -u root -p
```

```sql
CREATE DATABASE accounting CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE accounting;
```

### Використовуючи phpMyAdmin

1. Відкрийте phpMyAdmin
2. Натисніть на вкладку **"Бази даних"**
3. Введіть назву: `accounting`
4. Оберіть кодування: `utf8mb4_unicode_ci`
5. Натисніть **"Створити"**

### Використовуючи MySQL Workbench

1. Підключіться до сервера MySQL
2. Натисніть правою кнопкою на **"Schemas"**
3. Оберіть **"Create Schema"**
4. Назва: `accounting`
5. Charset: `utf8mb4`
6. Натисніть **"Apply"**

---

## Крок 2: Створення таблиць

Виконайте наступні SQL команди по черзі:

### 2.1. Таблиця Programs (Програми)

```sql
CREATE TABLE Programs (
    id INT PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(255) NOT NULL CHECK (CHAR_LENGTH(title) >= 3),
    UNIQUE KEY unique_title (title)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**Опис:** Зберігає інформацію про освітні програми.

### 2.2. Таблиця Groups (Групи)

```sql
CREATE TABLE `Groups` (
    id INT PRIMARY KEY AUTO_INCREMENT,
    code VARCHAR(50) NOT NULL CHECK (code REGEXP '^[A-Z]{2,4}-[0-9]{2}-[0-9]+$'),
    program_id INT NOT NULL,
    UNIQUE KEY unique_code (code),
    FOREIGN KEY (program_id) REFERENCES Programs(id) ON DELETE CASCADE ON UPDATE CASCADE,
    INDEX idx_program (program_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**Опис:** Зберігає інформацію про навчальні групи з прив'язкою до програм.

⚠️ **Важливо:** Назва таблиці `Groups` береться в зворотні лапки, оскільки це зарезервоване слово в MySQL.

### 2.3. Таблиця Students (Студенти)

```sql
CREATE TABLE Students (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(255) NOT NULL CHECK (CHAR_LENGTH(name) >= 5),
    birth_date DATE NOT NULL CHECK (birth_date BETWEEN '1990-01-01' AND CURDATE()),
    funding_type ENUM('state', 'contract') NOT NULL,
    group_id INT NOT NULL,
    FOREIGN KEY (group_id) REFERENCES `Groups`(id) ON DELETE RESTRICT ON UPDATE CASCADE,
    INDEX idx_group (group_id),
    INDEX idx_funding (funding_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**Опис:** Зберігає основну інформацію про студентів.

### 2.4. Таблиця Addresses (Адреси)

```sql
CREATE TABLE Addresses (
    id INT PRIMARY KEY AUTO_INCREMENT,
    student_id INT NOT NULL UNIQUE,
    city VARCHAR(100) NOT NULL CHECK (CHAR_LENGTH(city) >= 2),
    street VARCHAR(255) DEFAULT NULL,
    postal_code VARCHAR(10) DEFAULT NULL CHECK (postal_code IS NULL OR postal_code REGEXP '^[0-9]{5}$'),
    FOREIGN KEY (student_id) REFERENCES Students(id) ON DELETE CASCADE ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**Опис:** Зберігає адресну інформацію студентів (зв'язок 1:1 зі Students).

---

## Крок 3: Завантаження тестових даних

### 3.1. Додавання програм

```sql
INSERT INTO Programs (id, title) VALUES 
    (1, 'Кібербезпека'),
    (2, 'Інженерія ПЗ'),
    (3, 'Комп''ютерні науки');
```

**Результат:** Створено 3 освітні програми.

### 3.2. Додавання груп

```sql
INSERT INTO `Groups` (id, code, program_id) VALUES
    (1, 'KB-25-1', 1),
    (2, 'IPZ-25-1', 2),
    (3, 'CS-25-1', 3);
```

**Результат:** Створено 3 групи, прив'язані до програм.

### 3.3. Додавання студентів

```sql
INSERT INTO Students (id, name, birth_date, funding_type, group_id) VALUES
    (1, 'Анна Коваль', '2007-03-14', 'contract', 1),
    (2, 'Іван Петренко', '2006-11-02', 'state', 1),
    (3, 'Марія Бондар', '2007-08-22', 'state', 2),
    (4, 'Олег Сидоренко', '2006-01-30', 'contract', 2),
    (5, 'Петро Шевченко', '2007-05-18', 'state', 3);
```

**Результат:** Створено 5 студентів з різними типами фінансування.

### 3.4. Додавання адрес

```sql
INSERT INTO Addresses (id, student_id, city, street, postal_code) VALUES
    (1, 1, 'Чернігів', 'вул. Шевченка, 10', '14000'),
    (2, 2, 'Київ', 'просп. Перемоги, 25', '03056'),
    (3, 3, 'Львів', 'вул. Франка, 5', '79000'),
    (4, 4, 'Суми', 'вул. Соборна, 12', '40000');
```

**Результат:** Додано адреси для 4 з 5 студентів (Петро Шевченко без адреси).

---

## Крок 4: Перевірка встановлення

### 4.1. Перевірка таблиць

```sql
SHOW TABLES;
```

**Очікуваний результат:**
```
+----------------------+
| Tables_in_accounting |
+----------------------+
| Addresses            |
| Groups               |
| Programs             |
| Students             |
+----------------------+
```

### 4.2. Перевірка кількості записів

```sql
SELECT 'Programs' AS table_name, COUNT(*) AS count FROM Programs
UNION ALL
SELECT 'Groups', COUNT(*) FROM `Groups`
UNION ALL
SELECT 'Students', COUNT(*) FROM Students
UNION ALL
SELECT 'Addresses', COUNT(*) FROM Addresses;
```

**Очікуваний результат:**
```
+------------+-------+
| table_name | count |
+------------+-------+
| Programs   |     3 |
| Groups     |     3 |
| Students   |     5 |
| Addresses  |     4 |
+------------+-------+
```

### 4.3. Перевірка зв'язків

```sql
SELECT 
    s.name AS student_name,
    g.code AS group_code,
    p.title AS program_title,
    s.funding_type,
    a.city
FROM Students s
JOIN `Groups` g ON s.group_id = g.id
JOIN Programs p ON g.program_id = p.id
LEFT JOIN Addresses a ON s.id = a.student_id
ORDER BY s.id;
```

**Очікуваний результат:**
```
+-----------------+------------+--------------------+--------------+-----------+
| student_name    | group_code | program_title      | funding_type | city      |
+-----------------+------------+--------------------+--------------+-----------+
| Анна Коваль     | KB-25-1    | Кібербезпека       | contract     | Чернігів  |
| Іван Петренко   | KB-25-1    | Кібербезпека       | state        | Київ      |
| Марія Бондар    | IPZ-25-1   | Інженерія ПЗ       | state        | Львів     |
| Олег Сидоренко  | IPZ-25-1   | Інженерія ПЗ       | contract     | Суми      |
| Петро Шевченко  | CS-25-1    | Комп'ютерні науки  | state        | NULL      |
+-----------------+------------+--------------------+--------------+-----------+
```

✅ **Якщо всі перевірки пройшли успішно - система готова до роботи!**

---

## Варіанти встановлення

### 📱 Варіант 1: phpMyAdmin (Рекомендований для початківців)

**Переваги:**
- ✅ Графічний інтерфейс
- ✅ Не потрібно знати командний рядок
- ✅ Вбудовані підказки

**Як використовувати:**
1. Відкрийте phpMyAdmin в браузері
2. Створіть базу даних через інтерфейс
3. Використовуйте вкладку **"SQL"** для виконання команд
4. Копіюйте та виконуйте скрипти по черзі

### 💻 Варіант 2: MySQL CLI (Для досвідчених користувачів)

**Переваги:**
- ✅ Швидке виконання
- ✅ Можна автоматизувати
- ✅ Доступний на всіх платформах

**Як використовувати:**

1. **Windows:**
```bash
"C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe" -u root -p
```

2. **Linux/Mac:**
```bash
mysql -u root -p
```

3. **Виконання з файлу:**
```bash
mysql -u root -p accounting < setup.sql
```

### 🔧 Варіант 3: MySQL Workbench (Для розробників)

**Переваги:**
- ✅ Потужні інструменти для роботи з БД
- ✅ Візуальний конструктор
- ✅ Відладка запитів

**Як використовувати:**
1. Підключіться до MySQL Server
2. Створіть нову схему (Schema)
3. Відкрийте SQL редактор (⚡ значок)
4. Вставте та виконайте SQL команди

---

## 🆘 Вирішення проблем

### Проблема: "Table 'Groups' doesn't exist"

**Рішення:** Використовуйте зворотні лапки:
```sql
SELECT * FROM `Groups`;
```

### Проблема: "Access denied for user"

**Рішення:** Перевірте права доступу:
```sql
GRANT ALL PRIVILEGES ON accounting.* TO 'your_user'@'localhost';
FLUSH PRIVILEGES;
```

### Проблема: "Check constraint is violated"

**Рішення:** Перевірте, чи дані відповідають обмеженням:
- title >= 3 символи
- code формат: XX-00-0 (наприклад, KB-25-1)
- birth_date між 1990-01-01 та сьогодні

Більше рішень: [Обробка помилок](ERROR_HANDLING.md)

---

## 📚 Наступні кроки

Після успішного встановлення:

1. 📖 Ознайомтеся зі [Структурою БД](DATABASE_STRUCTURE.md)
2. ⚙️ Вивчіть [CRUD операції](CRUD_OPERATIONS.md)
3. 📝 Перегляньте [Типові сценарії](USE_CASES.md)
4. 🧪 Виконайте [Тестування](TESTING.md)

---

## 🔗 Корисні посилання

- [Головна сторінка](../README.md)
- [Структура БД](DATABASE_STRUCTURE.md)
- [CRUD операції](CRUD_OPERATIONS.md)
- [FAQ](FAQ.md)

---

**Версія:** 1.0  
**Дата оновлення:** 2025-12-08
