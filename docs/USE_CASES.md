# 📝 Типові сценарії використання

Приклади реальних сценаріїв роботи з системою обліку студентів.

## 📑 Зміст

- [Сценарій 1: Зарахування нового студента](#сценарій-1-зарахування-нового-студента)
- [Сценарій 2: Переведення студента на контракт](#сценарій-2-переведення-студента-на-контракт)
- [Сценарій 3: Переведення студента в іншу групу](#сценарій-3-переведення-студента-в-іншу-групу)
- [Сценарій 4: Відрахування студента](#сценарій-4-відрахування-студента)
- [Сценарій 5: Створення нової групи з програмою](#сценарій-5-створення-нової-групи-з-програмою)
- [Сценарій 6: Звіт про студентів на бюджеті](#сценарій-6-звіт-про-студентів-на-бюджеті)
- [Сценарій 7: Пошук студентів з певного міста](#сценарій-7-пошук-студентів-з-певного-міста)
- [Сценарій 8: Статистика по групах](#сценарій-8-статистика-по-групах)

---

## Сценарій 1: Зарахування нового студента

### 📋 Опис задачі

Потрібно зарахувати нового студента Дмитра Іваненка до групи KB-25-1 на державне фінансування та додати його адресу в Одесі.

### ✅ Покроковий процес

#### Крок 1: Перевірка групи

```sql
-- Перевіряємо, чи існує група KB-25-1
SELECT g.id, g.code, p.title 
FROM `Groups` g
JOIN Programs p ON g.program_id = p.id
WHERE g.code = 'KB-25-1';
```

**Очікуваний результат:**
```
+----+---------+------------------+
| id | code    | title            |
+----+---------+------------------+
|  1 | KB-25-1 | Кібербезпека     |
+----+---------+------------------+
```

✅ Група існує, можемо продовжувати.

#### Крок 2: Додавання студента

```sql
-- Додаємо студента
INSERT INTO Students (name, birth_date, funding_type, group_id) 
VALUES ('Дмитро Іваненко', '2007-07-25', 'state', 1);
```

**Результат:**
```
Query OK, 1 row affected (0.01 sec)
```

#### Крок 3: Збереження ID нового студента

```sql
-- Отримуємо ID щойно доданого студента
SELECT LAST_INSERT_ID() AS new_student_id;
```

**Результат:**
```
+-----------------+
| new_student_id  |
+-----------------+
|               6 |
+-----------------+
```

Зберігаємо ID = 6 для наступного кроку.

#### Крок 4: Додавання адреси

```sql
-- Додаємо адресу для студента з ID = 6
INSERT INTO Addresses (student_id, city, street, postal_code) 
VALUES (6, 'Одеса', 'вул. Дерибасівська, 10', '65000');
```

**Результат:**
```
Query OK, 1 row affected (0.01 sec)
```

#### Крок 5: Перевірка результату

```sql
-- Перевіряємо повну інформацію про нового студента
SELECT 
    s.id,
    s.name,
    s.birth_date,
    YEAR(CURDATE()) - YEAR(s.birth_date) AS age,
    CASE 
        WHEN s.funding_type = 'state' THEN 'Бюджет'
        WHEN s.funding_type = 'contract' THEN 'Контракт'
    END AS funding,
    g.code AS group_code,
    p.title AS program,
    a.city,
    a.street,
    a.postal_code
FROM Students s
JOIN `Groups` g ON s.group_id = g.id
JOIN Programs p ON g.program_id = p.id
LEFT JOIN Addresses a ON s.id = a.student_id
WHERE s.id = 6;
```

**Очікуваний результат:**
```
+----+------------------+------------+-----+---------+------------+------------------+---------+---------------------------+-------------+
| id | name             | birth_date | age | funding | group_code | program          | city    | street                    | postal_code |
+----+------------------+------------+-----+---------+------------+------------------+---------+---------------------------+-------------+
|  6 | Дмитро Іваненко  | 2007-07-25 |  18 | Бюджет  | KB-25-1    | Кібербезпека     | Одеса   | вул. Дерибасівська, 10    | 65000       |
+----+------------------+------------+-----+---------+------------+------------------+---------+---------------------------+-------------+
```

✅ **Успіх!** Студент успішно зарахований.

### 🔄 Альтернативний варіант (з транзакцією)

```sql
-- Використання транзакції для забезпечення цілісності
START TRANSACTION;

-- Додаємо студента
INSERT INTO Students (name, birth_date, funding_type, group_id) 
VALUES ('Дмитро Іваненко', '2007-07-25', 'state', 1);

-- Зберігаємо ID
SET @student_id = LAST_INSERT_ID();

-- Додаємо адресу
INSERT INTO Addresses (student_id, city, street, postal_code) 
VALUES (@student_id, 'Одеса', 'вул. Дерибасівська, 10', '65000');

-- Якщо все добре, зберігаємо зміни
COMMIT;

-- Перевірка
SELECT s.name, g.code, a.city 
FROM Students s 
JOIN `Groups` g ON s.group_id = g.id 
LEFT JOIN Addresses a ON s.id = a.student_id 
WHERE s.id = @student_id;
```

---

## Сценарій 2: Переведення студента на контракт

### 📋 Опис задачі

Студент Іван Петренко (ID = 2) втратив право на державне фінансування і переводиться на контракт.

### ✅ Покроковий процес

#### Крок 1: Перевірка поточного статусу

```sql
SELECT 
    s.id,
    s.name,
    CASE 
        WHEN s.funding_type = 'state' THEN 'Бюджет'
        WHEN s.funding_type = 'contract' THEN 'Контракт'
    END AS current_funding
FROM Students s
WHERE s.id = 2;
```

**Результат:**
```
+----+-----------------+-----------------+
| id | name            | current_funding |
+----+-----------------+-----------------+
|  2 | Іван Петренко   | Бюджет          |
+----+-----------------+-----------------+
```

#### Крок 2: Переведення на контракт

```sql
UPDATE Students 
SET funding_type = 'contract' 
WHERE id = 2;
```

**Результат:**
```
Query OK, 1 row affected (0.01 sec)
Rows matched: 1  Changed: 1  Warnings: 0
```

#### Крок 3: Перевірка результату

```sql
SELECT 
    s.id,
    s.name,
    CASE 
        WHEN s.funding_type = 'state' THEN 'Бюджет'
        WHEN s.funding_type = 'contract' THEN 'Контракт'
    END AS new_funding
FROM Students s
WHERE s.id = 2;
```

**Результат:**
```
+----+-----------------+-------------+
| id | name            | new_funding |
+----+-----------------+-------------+
|  2 | Іван Петренко   | Контракт    |
+----+-----------------+-------------+
```

✅ **Успіх!** Тип фінансування змінено.

### 📊 Переведення всієї групи

```sql
-- Переведення всіх студентів групи KB-25-1 на контракт
UPDATE Students 
SET funding_type = 'contract' 
WHERE group_id = (SELECT id FROM `Groups` WHERE code = 'KB-25-1');

-- Перевірка результату
SELECT s.name, s.funding_type, g.code
FROM Students s
JOIN `Groups` g ON s.group_id = g.id
WHERE g.code = 'KB-25-1';
```

---

## Сценарій 3: Переведення студента в іншу групу

### 📋 Опис задачі

Студентка Анна Коваль (ID = 1) переводиться з групи KB-25-1 в групу CS-25-1 (зміна спеціальності).

### ✅ Покроковий процес

#### Крок 1: Перевірка поточної групи

```sql
SELECT 
    s.id,
    s.name,
    g.code AS current_group,
    p.title AS current_program
FROM Students s
JOIN `Groups` g ON s.group_id = g.id
JOIN Programs p ON g.program_id = p.id
WHERE s.id = 1;
```

**Результат:**
```
+----+-------------+---------------+------------------+
| id | name        | current_group | current_program  |
+----+-------------+---------------+------------------+
|  1 | Анна Коваль | KB-25-1       | Кібербезпека     |
+----+-------------+---------------+------------------+
```

#### Крок 2: Перевірка нової групи

```sql
SELECT 
    g.id,
    g.code,
    p.title,
    COUNT(s.id) AS students_count
FROM `Groups` g
JOIN Programs p ON g.program_id = p.id
LEFT JOIN Students s ON g.id = s.group_id
WHERE g.code = 'CS-25-1'
GROUP BY g.id, g.code, p.title;
```

**Результат:**
```
+----+---------+---------------------+----------------+
| id | code    | title               | students_count |
+----+---------+---------------------+----------------+
|  3 | CS-25-1 | Комп'ютерні науки   |              1 |
+----+---------+---------------------+----------------+
```

#### Крок 3: Переведення

```sql
UPDATE Students 
SET group_id = 3 
WHERE id = 1;
```

**Результат:**
```
Query OK, 1 row affected (0.01 sec)
```

#### Крок 4: Перевірка результату

```sql
SELECT 
    s.id,
    s.name,
    g.code AS new_group,
    p.title AS new_program
FROM Students s
JOIN `Groups` g ON s.group_id = g.id
JOIN Programs p ON g.program_id = p.id
WHERE s.id = 1;
```

**Результат:**
```
+----+-------------+-----------+---------------------+
| id | name        | new_group | new_program         |
+----+-------------+-----------+---------------------+
|  1 | Анна Коваль | CS-25-1   | Комп'ютерні науки   |
+----+-------------+-----------+---------------------+
```

✅ **Успіх!** Студентка переведена.

---

## Сценарій 4: Відрахування студента

### 📋 Опис задачі

Студент Петро Шевченко (ID = 5) відраховується з навчального закладу.

### ✅ Покроковий процес

#### Крок 1: Перевірка інформації про студента

```sql
SELECT 
    s.id,
    s.name,
    g.code AS group_code,
    p.title AS program,
    a.city,
    a.street
FROM Students s
JOIN `Groups` g ON s.group_id = g.id
JOIN Programs p ON g.program_id = p.id
LEFT JOIN Addresses a ON s.id = a.student_id
WHERE s.id = 5;
```

**Результат:**
```
+----+----------------+------------+---------------------+------+--------+
| id | name           | group_code | program             | city | street |
+----+----------------+------------+---------------------+------+--------+
|  5 | Петро Шевченко | CS-25-1    | Комп'ютерні науки   | NULL | NULL   |
+----+----------------+------------+---------------------+------+--------+
```

#### Крок 2: Видалення студента

```sql
-- Видаляємо студента (адреса видалиться автоматично через CASCADE)
DELETE FROM Students WHERE id = 5;
```

**Результат:**
```
Query OK, 1 row affected (0.01 sec)
```

⚠️ **Примітка:** Якщо у студента була адреса, вона також буде видалена через `ON DELETE CASCADE`.

#### Крок 3: Перевірка результату

```sql
-- Перевіряємо, що студент видалений
SELECT * FROM Students WHERE id = 5;
```

**Результат:**
```
Empty set (0.00 sec)
```

✅ **Успіх!** Студент відрахований.

#### Крок 4: Перевірка статистики групи

```sql
SELECT 
    g.code,
    COUNT(s.id) AS students_count
FROM `Groups` g
LEFT JOIN Students s ON g.id = s.group_id
WHERE g.code = 'CS-25-1'
GROUP BY g.code;
```

**Результат:**
```
+---------+----------------+
| code    | students_count |
+---------+----------------+
| CS-25-1 |              0 |
+---------+----------------+
```

---

## Сценарій 5: Створення нової групи з програмою

### 📋 Опис задачі

Потрібно створити нову освітню програму "Штучний інтелект" та групу AI-25-1.

### ✅ Покроковий процес

#### Крок 1: Створення програми

```sql
INSERT INTO Programs (title) 
VALUES ('Штучний інтелект');
```

**Результат:**
```
Query OK, 1 row affected (0.01 sec)
```

#### Крок 2: Отримання ID нової програми

```sql
SELECT LAST_INSERT_ID() AS program_id;
```

**Результат:**
```
+------------+
| program_id |
+------------+
|          4 |
+------------+
```

#### Крок 3: Створення групи

```sql
INSERT INTO `Groups` (code, program_id) 
VALUES ('AI-25-1', 4);
```

**Результат:**
```
Query OK, 1 row affected (0.01 sec)
```

#### Крок 4: Перевірка результату

```sql
SELECT 
    g.id,
    g.code,
    p.title AS program
FROM `Groups` g
JOIN Programs p ON g.program_id = p.id
WHERE p.id = 4;
```

**Результат:**
```
+----+---------+--------------------+
| id | code    | program            |
+----+---------+--------------------+
|  4 | AI-25-1 | Штучний інтелект   |
+----+---------+--------------------+
```

✅ **Успіх!** Програма та група створені.

### 🔄 Варіант з транзакцією

```sql
START TRANSACTION;

-- Створюємо програму
INSERT INTO Programs (title) VALUES ('Штучний інтелект');
SET @program_id = LAST_INSERT_ID();

-- Створюємо кілька груп для цієї програми
INSERT INTO `Groups` (code, program_id) VALUES 
    ('AI-25-1', @program_id),
    ('AI-25-2', @program_id);

-- Зберігаємо зміни
COMMIT;

-- Перевірка
SELECT g.code, p.title 
FROM `Groups` g 
JOIN Programs p ON g.program_id = p.id 
WHERE p.id = @program_id;
```

---

## Сценарій 6: Звіт про студентів на бюджеті

### 📋 Опис задачі

Необхідно підготувати звіт про всіх студентів, які навчаються на державному фінансуванні, з групуванням за програмами та групами.

### ✅ SQL запит

```sql
SELECT 
    p.title AS program,
    g.code AS group_code,
    COUNT(s.id) AS students_on_budget,
    GROUP_CONCAT(s.name ORDER BY s.name SEPARATOR ', ') AS student_names
FROM Programs p
JOIN `Groups` g ON p.id = g.program_id
LEFT JOIN Students s ON g.id = s.group_id AND s.funding_type = 'state'
GROUP BY p.id, p.title, g.id, g.code
HAVING students_on_budget > 0
ORDER BY p.title, g.code;
```

**Очікуваний результат:**
```
+---------------------+------------+--------------------+------------------------------------+
| program             | group_code | students_on_budget | student_names                      |
+---------------------+------------+--------------------+------------------------------------+
| Інженерія ПЗ        | IPZ-25-1   |                  1 | Марія Бондар                       |
| Кібербезпека        | KB-25-1    |                  1 | Іван Петренко                      |
| Комп'ютерні науки   | CS-25-1    |                  1 | Петро Шевченко                     |
+---------------------+------------+--------------------+------------------------------------+
```

### 📊 Розширений звіт з адресами

```sql
SELECT 
    p.title AS program,
    g.code AS group_code,
    s.name AS student_name,
    s.birth_date,
    YEAR(CURDATE()) - YEAR(s.birth_date) AS age,
    COALESCE(a.city, 'Адреса не вказана') AS city
FROM Programs p
JOIN `Groups` g ON p.id = g.program_id
JOIN Students s ON g.id = s.group_id
LEFT JOIN Addresses a ON s.id = a.student_id
WHERE s.funding_type = 'state'
ORDER BY p.title, g.code, s.name;
```

### 📈 Статистика по програмах

```sql
SELECT 
    p.title AS program,
    COUNT(DISTINCT g.id) AS groups_count,
    COUNT(s.id) AS total_students,
    SUM(CASE WHEN s.funding_type = 'state' THEN 1 ELSE 0 END) AS budget_students,
    SUM(CASE WHEN s.funding_type = 'contract' THEN 1 ELSE 0 END) AS contract_students,
    ROUND(SUM(CASE WHEN s.funding_type = 'state' THEN 1 ELSE 0 END) * 100.0 / COUNT(s.id), 2) AS budget_percentage
FROM Programs p
LEFT JOIN `Groups` g ON p.id = g.program_id
LEFT JOIN Students s ON g.id = s.group_id
GROUP BY p.id, p.title
ORDER BY p.title;
```

---

## Сценарій 7: Пошук студентів з певного міста

### 📋 Опис задачі

Знайти всіх студентів, які проживають у Києві, з повною інформацією про їх навчання.

### ✅ SQL запит

```sql
SELECT 
    s.id,
    s.name,
    s.birth_date,
    CASE 
        WHEN s.funding_type = 'state' THEN 'Бюджет'
        WHEN s.funding_type = 'contract' THEN 'Контракт'
    END AS funding,
    g.code AS group_code,
    p.title AS program,
    a.city,
    a.street,
    a.postal_code
FROM Students s
JOIN `Groups` g ON s.group_id = g.id
JOIN Programs p ON g.program_id = p.id
JOIN Addresses a ON s.id = a.student_id
WHERE a.city = 'Київ'
ORDER BY s.name;
```

**Очікуваний результат:**
```
+----+-----------------+------------+----------+------------+------------------+-------+------------------------+-------------+
| id | name            | birth_date | funding  | group_code | program          | city  | street                 | postal_code |
+----+-----------------+------------+----------+------------+------------------+-------+------------------------+-------------+
|  2 | Іван Петренко   | 2006-11-02 | Бюджет   | KB-25-1    | Кібербезпека     | Київ  | просп. Перемоги, 25    | 03056       |
+----+-----------------+------------+----------+------------+------------------+-------+------------------------+-------------+
```

### 📊 Статистика по містах

```sql
SELECT 
    a.city,
    COUNT(s.id) AS students_count,
    GROUP_CONCAT(s.name ORDER BY s.name SEPARATOR ', ') AS students
FROM Addresses a
JOIN Students s ON a.student_id = s.id
GROUP BY a.city
ORDER BY students_count DESC, a.city;
```

**Очікуваний результат:**
```
+-----------+----------------+-------------------+
| city      | students_count | students          |
+-----------+----------------+-------------------+
| Київ      |              1 | Іван Петренко     |
| Львів     |              1 | Марія Бондар      |
| Суми      |              1 | Олег Сидоренко    |
| Чернігів  |              1 | Анна Коваль       |
+-----------+----------------+-------------------+
```

---

## Сценарій 8: Статистика по групах

### 📋 Опис задачі

Отримати детальну статистику по кожній групі: кількість студентів, співвідношення бюджет/контракт, середній вік.

### ✅ SQL запит

```sql
SELECT 
    g.code AS group_code,
    p.title AS program,
    COUNT(s.id) AS total_students,
    SUM(CASE WHEN s.funding_type = 'state' THEN 1 ELSE 0 END) AS budget_count,
    SUM(CASE WHEN s.funding_type = 'contract' THEN 1 ELSE 0 END) AS contract_count,
    ROUND(AVG(YEAR(CURDATE()) - YEAR(s.birth_date)), 1) AS average_age,
    MIN(s.birth_date) AS oldest_student,
    MAX(s.birth_date) AS youngest_student
FROM `Groups` g
JOIN Programs p ON g.program_id = p.id
LEFT JOIN Students s ON g.id = s.group_id
GROUP BY g.id, g.code, p.title
ORDER BY g.code;
```

**Очікуваний результат:**
```
+------------+---------------------+----------------+--------------+----------------+-------------+----------------+------------------+
| group_code | program             | total_students | budget_count | contract_count | average_age | oldest_student | youngest_student |
+------------+---------------------+----------------+--------------+----------------+-------------+----------------+------------------+
| CS-25-1    | Комп'ютерні науки   |              1 |            1 |              0 |        17.6 | 2007-05-18     | 2007-05-18       |
| IPZ-25-1   | Інженерія ПЗ        |              2 |            1 |              1 |        18.3 | 2006-01-30     | 2007-08-22       |
| KB-25-1    | Кібербезпека        |              2 |            1 |              1 |        18.1 | 2006-11-02     | 2007-03-14       |
+------------+---------------------+----------------+--------------+----------------+-------------+----------------+------------------+
```

### 📊 Детальна інформація про студентів групи

```sql
-- Вибрати конкретну групу для детального аналізу
SET @group_code = 'KB-25-1';

SELECT 
    s.id,
    s.name,
    YEAR(CURDATE()) - YEAR(s.birth_date) AS age,
    CASE 
        WHEN s.funding_type = 'state' THEN 'Бюджет'
        WHEN s.funding_type = 'contract' THEN 'Контракт'
    END AS funding,
    COALESCE(a.city, 'Не вказано') AS city,
    CASE 
        WHEN a.id IS NOT NULL THEN 'Є'
        ELSE 'Немає'
    END AS has_address
FROM Students s
JOIN `Groups` g ON s.group_id = g.id
LEFT JOIN Addresses a ON s.id = a.student_id
WHERE g.code = @group_code
ORDER BY s.name;
```

### 📈 Порівняльна статистика

```sql
SELECT 
    'Загалом студентів' AS metric,
    COUNT(*) AS value
FROM Students

UNION ALL

SELECT 
    'На бюджеті',
    COUNT(*) 
FROM Students 
WHERE funding_type = 'state'

UNION ALL

SELECT 
    'На контракті',
    COUNT(*) 
FROM Students 
WHERE funding_type = 'contract'

UNION ALL

SELECT 
    'З адресами',
    COUNT(DISTINCT student_id) 
FROM Addresses

UNION ALL

SELECT 
    'Без адрес',
    COUNT(*) 
FROM Students s 
WHERE NOT EXISTS (SELECT 1 FROM Addresses a WHERE a.student_id = s.id);
```

---

## 🔗 Корисні посилання

- [Головна сторінка](../README.md)
- [Швидкий старт](QUICK_START.md)
- [Структура БД](DATABASE_STRUCTURE.md)
- [CRUD операції](CRUD_OPERATIONS.md)
- [FAQ](FAQ.md)

---

**Версія:** 1.0  
**Дата оновлення:** 2025-12-08
